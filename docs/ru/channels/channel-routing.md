---
summary: "Правила маршрутизации для каждого канала (WhatsApp, Telegram, Discord, Slack) и общий контекст"
read_when:
  - При изменении маршрутизации каналов или поведения входящих сообщений
title: "Маршрутизация каналов"
---

# Каналы и маршрутизация

OpenClaw направляет ответы **обратно в тот канал, из которого пришло сообщение**. Модель не выбирает канал: маршрутизация детерминирована и управляется конфигурацией хоста.

## Основные термины

- **Канал**: `telegram`, `whatsapp`, `discord`, `irc`, `googlechat`, `slack`, `signal`, `imessage`, `line`, а также каналы-расширения. `webchat` — внутренний канал пользовательского интерфейса WebChat, он не является настраиваемым исходящим каналом.
- **AccountId**: экземпляр учётной записи для конкретного канала (если поддерживается).
- Опциональная учётная запись по умолчанию для канала: параметр `channels.<channel>.defaultAccount` определяет, какая учётная запись будет использоваться, если в исходящем пути не указан `accountId`.
  - В настройках с несколькими учётными записями укажите явную учётную запись по умолчанию (`defaultAccount` или `accounts.default`), если настроено две или более учётных записей. Без этого резервная маршрутизация может выбрать первый нормализованный идентификатор учётной записи.
- **AgentId**: изолированное рабочее пространство + хранилище сессий ("мозг").
- **SessionKey**: ключ корзины, используемый для хранения контекста и управления параллелизмом.

## Формы ключей сессий (примеры)

Прямые сообщения сворачиваются в **основную** сессию агента:

- `agent:<agentId>:<mainKey>` (по умолчанию: `agent:main:main`)

Группы и каналы остаются изолированными для каждого канала:

- Группы: `agent:<agentId>:<channel>:group:<id>`
- Каналы/комнаты: `agent:<agentId>:<channel>:channel:<id>`

Темы:

- В темах Slack/Discord к базовому ключу добавляется `:thread:<threadId>`.
- В темах форума Telegram в ключ группы встраивается `:topic:<topicId>`.

Примеры:

- `agent:main:telegram:group:-1001234567890:topic:42`
- `agent:main:discord:channel:123456:thread:987654`

## Закрепление основного маршрута для прямых сообщений

Когда `session.dmScope` имеет значение `main`, прямые сообщения могут использовать одну основную сессию. Чтобы предотвратить перезапись `lastRoute` сессии сообщениями от пользователей, не являющихся владельцами, OpenClaw определяет закреплённого владельца на основе `allowFrom` при выполнении всех следующих условий:

- В `allowFrom` есть ровно одна запись без подстановочного знака.
- Запись может быть нормализована до конкретного идентификатора отправителя для этого канала.
- Отправитель входящего прямого сообщения не совпадает с закреплённым владельцем.

В случае несовпадения OpenClaw по-прежнему записывает метаданные входящей сессии, но пропускает обновление `lastRoute` основной сессии.

## Правила маршрутизации (как выбирается агент)

Маршрутизация выбирает **одного агента** для каждого входящего сообщения:

1. **Точное совпадение участника** (`bindings` с `peer.kind` + `peer.id`).
2. **Совпадение родительского участника** (наследование темы).
3. **Совпадение гильдии и ролей** (Discord) через `guildId` + `roles`.
4. **Совпадение гильдии** (Discord) через `guildId`.
5. **Совпадение команды** (Slack) через `teamId`.
6. **Совпадение учётной записи** (`accountId` в канале).
7. **Совпадение канала** (любая учётная запись в этом канале, `accountId: "*"`).
8. **Агент по умолчанию** (`agents.list[].default`, в противном случае — первая запись в списке, резервный вариант — `main`).

Если привязка включает несколько полей сопоставления (`peer`, `guildId`, `teamId`, `roles`), **все указанные поля должны совпадать**, чтобы эта привязка применялась.

Выбранный агент определяет, какое рабочее пространство и хранилище сессий будут использоваться.

## Группы рассылки (запуск нескольких агентов)

Группы рассылки позволяют запускать **несколько агентов** для одного и того же участника, **когда OpenClaw обычно отвечает** (например, в группах WhatsApp после упоминания/активации).

Конфигурация:

```json5
{
  broadcast: {
    strategy: "parallel",
    "120363403215116621@g.us": ["alfred", "baerbel"],
    "+15555550123": ["support", "logger"],
  },
}
```

См.: [Группы рассылки](/channels/broadcast-groups).

## Обзор конфигурации

- `agents.list`: именованные определения агентов (рабочее пространство, модель и т. д.).
- `bindings`: сопоставление входящих каналов/учётных записей/участников с агентами.

Пример:

```json5
{
  agents: {
    list: [{ id: "support", name: "Support", workspace: "~/.openclaw/workspace-support" }],
  },
  bindings: [
    { match: { channel: "slack", teamId: "T123" }, agentId: "support" },
    { match: { channel: "telegram", peer: { kind: "group", id: "-100123" } }, agentId: "support" },
  ],
}
```

## Хранение сессий

Хранилища сессий располагаются в каталоге состояния (по умолчанию `~/.openclaw`):

- `~/.openclaw/agents/<agentId>/sessions/sessions.json`
- Транскрипты в формате JSONL находятся рядом с хранилищем

Вы можете переопределить путь к хранилищу с помощью `session.store` и шаблонизации `{agentId}`.

Шлюз и обнаружение сессий ACP также сканируют хранилища агентов на диске в корне по умолчанию `agents/` и в шаблонизированных корнях `session.store`. Обнаруженные хранилища должны находиться внутри разрешённого корневого каталога агента и использовать обычный файл `sessions.json`. Символьные ссылки и пути вне корневого каталога игнорируются.

## Поведение WebChat

WebChat подключается к **выбранному агенту** и по умолчанию использует основную сессию агента. Благодаря этому в WebChat можно увидеть межканальный контекст для этого агента в одном месте.

## Контекст ответа

Входящие ответы включают:

- `ReplyToId`, `ReplyToBody` и `ReplyToSender`, если они доступны.
- Цитируемый контекст добавляется к `Body` в виде блока `[Replying to ...]`.

Это единообразно для всех каналов.